// Created by Angelo John Benedict Laus - BSCPE 231

#include <iostream>
#include <string>

using namespace std;

int main() {

    /* Declaring and initializing */
    float x, y;

    cout << "Please input value for X: ";
    cin >> x;
    cout << "Please input value for Y: ";
    cin >> y;

    /* Calculating of values */
    cout << "\nThe sum for X and Y is " << x + y << endl;
    cout << "The difference between X and Y is " << x - y << endl;
    cout << "The product for X and Y is " << x * y << endl;
    cout << "The quotient for X divide it by Y is " << x / y << endl;

    return 0;

}